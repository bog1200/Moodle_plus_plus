export const metadata = {
    title: "Courses | Moodle++",
    description: "Courses for Moodle++",
}


export default async function CoursesLayout({children}: {children: React.ReactNode}) {
    return (
        <>
            {children}
        </>
    );
}